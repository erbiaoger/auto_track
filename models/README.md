# models

Training outputs and checkpoints are written here.

- `track_slot_cuda/`: default TrackSlotNet CUDA training output.
- `track_slot_cpu/`: default CPU smoke-test output.
- `trajectory_query_online_v1_cuda/`: legacy query-model output when present.

Checkpoint files are generated artifacts and are ignored by git.
