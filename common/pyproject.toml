[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "auto-track-common"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["numpy>=2.4.2", "scipy>=1.17.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/auto_track_common"]
