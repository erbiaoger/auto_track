# Unified replay frontend

The Vite source is retained at
`hybrid_vehicle_tracker_20260724/web/frontend` so the existing local build and
static-serving path remain compatible. The root `web/frontend` directory is
the public web boundary; build output is not duplicated while the repository
is being migrated.
