# Unified backend registry

`methods.yaml` is the versioned registry consumed by the lightweight FastAPI
coordinator. The coordinator implementation remains in the Hybrid package to
preserve its existing CLI/import paths.
