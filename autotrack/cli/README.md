# autotrack.cli

Command-line wrappers for classic extraction engines.

- `auto_track_parallel.py`: CPU parallel extraction entry point.
- `auto_track_gpu.py`: CUDA/CuPy extraction entry point.

Deep-learning inference is handled by `autotrack.dl.infer_trajectory_model`.
