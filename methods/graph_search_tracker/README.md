# Graph Search Tracker

This is the isolated `graph_search` algorithm project.

This is the original CuPy-enhanced GPU graph search and dynamic-programming
implementation. No learned checkpoint is used. The DAY11 adapter calls
`extract_all_gpu`, preserves real mapping positions and reports
`direction=reverse`.
