[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"
[project]
name = "graph-search-tracker"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
[tool.hatch.build.targets.wheel]
packages = ["src/graph_search_tracker"]
