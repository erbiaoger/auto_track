# Vehicle Peak-Set Transformer

Active web preset: `peakguided_crossing_curriculum_recall`.

The web worker reuses the current peak-guided decoder and its recall-oriented
quality gates on each 120-second Gauss window.  Older proposal/trace runs are
listed as legacy artifacts and are not exposed as web methods.
