# PeakSlotNet source boundary

The maintained implementation remains at `autotrack/dl/peak_slot_model.py` so
existing notebooks and imports keep working. This directory is the method
boundary for new adapters; large model code is intentionally not duplicated.
