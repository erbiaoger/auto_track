# PeakSlotNet

Active web preset: `peak_slot_v4_noisy_badch`.

The v5 line-only experiments are retained in `archive/catalog.yaml` because
they require an external waveform-line U-Net checkpoint.  The active preset
uses the direct Gauss-compatible v4 checkpoint and keeps graph fusion off.
