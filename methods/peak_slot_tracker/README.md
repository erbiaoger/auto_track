# PeakSlot Tracker

This is the isolated `peak_slot` algorithm project.

This project owns PeakSlotNet source, PeakSlot datasets, checkpoints and
prediction exports. The web preset uses the CUDA v4 noisy/bad-channel epoch
295 checkpoint and Gauss DAY11 input.
