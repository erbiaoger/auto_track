# Vehicle Peak-Set Tracker

This is the isolated `vehicle_peak_set` algorithm project.

This project owns the crossing-curriculum Peak-Set Transformer, its training
run and web-session exports. The worker loads epoch 236 on CUDA and consumes
the shared Gauss DAY11 mmap.
