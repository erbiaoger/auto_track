[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"
[project]
name = "vehicle-peak-set-tracker"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
[tool.hatch.build.targets.wheel]
packages = ["src/vehicle_peak_set_tracker"]
